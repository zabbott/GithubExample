using UnityEngine;

public class TheBestScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("HOLY SHIT BATMAN WE'RE IN 289");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
